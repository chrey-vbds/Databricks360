name: Deploy-IAC

on:
  push:
    branches:
      - dev
    paths-ignore:
      - '*'
  pull_request:
    branches:
      - dev




jobs:
  check-bicep:
    name: Check Bicep Syntax
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: echosecret
        run: echo ${{ env.BASE_NAME }}